# Install PHP Environment
use brew to install 

```shell
brew install php
```

install mysql
```shell
brew install mysql
```

install php environment

Herd or Mamp

install Table plus for MySQL database
